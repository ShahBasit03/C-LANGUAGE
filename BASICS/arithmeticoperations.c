#include <stdio.h> 
int main() {
  int x=5;
  int y=2;
  printf("%d",x+y); //%d means int 
  printf("\n%d",x-y);
  printf("\n%d",x*y);
  printf("\n%d",x/y);  //if we want the exact value with decimals just  use float and %f
  return 0;
} 