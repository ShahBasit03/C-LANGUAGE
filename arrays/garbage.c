#include <stdio.h>
int main() {
  int arr[4];
  printf("%d",arr[0]);
  return 0;
}

//here nothing is defined first so it just shows garbage 

// not garbage jusr shows the way of writing array 
// int i,a=2,b=3;
// int arr[2+3];
// for (i=0;i<a+b;i++);{
//   scanf("%d",&arr[i])
//   printf("\n%d,arr[i]")
// }
